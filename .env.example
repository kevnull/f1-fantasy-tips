# Required
ANTHROPIC_API_KEY=sk-ant-...

# Set automatically by Railway — do not set manually
# PORT=3001

# Optional: override where thymes.db is stored.
# In Railway: add a volume, mount it at /data, then set this.
# DATA_DIR=/data

# Set by Railway automatically in production
# NODE_ENV=production
